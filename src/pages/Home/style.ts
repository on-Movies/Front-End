import {styled} from 'styled-components';

export const StyledButtonFilterMain = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    font-weight: bold;
    

    button{
        background-color: var(--color-secound);
        width: 100px;
    }

`;